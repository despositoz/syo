# SYO — Show Your Opinion

Telegram Mini App для личного киноархива: найти просмотренный фильм, отложить его
и — в следующих этапах — оценить и сохранить впечатление.

Это **greenfield-реализация версии 1.0**. Prototype A (одностраничный `app.js` в
`kino/webapp/`) не является её основой: из него взяты только знания о TMDB-запросах
и список ошибок, которые здесь не повторяются.

---

## Быстрый старт

```bash
npm install
```

Скопируй `.env.example` в `.env` и вставь TMDB-токен:

```bash
cp .env.example .env
```

```bash
npm run dev
```

Приложение откроется на `http://localhost:5173` (и по сети — на IP машины, чтобы
открыть с телефона).

---

## Переменные окружения

| Переменная                | Обязательна | Описание                                                            |
| ------------------------- | ----------- | ------------------------------------------------------------------- |
| `VITE_TMDB_API_KEY`       | да\*        | Ключ TMDB API v3 (read-only).                                       |
| `VITE_TMDB_ACCESS_TOKEN`  | нет         | Токен TMDB v4 (Bearer). Если задан — используется вместо v3.        |
| `VITE_TMDB_API_BASE`      | нет         | Другой базовый URL API (например, собственный прокси).              |
| `VITE_TMDB_IMAGE_BASE`    | нет         | Другой базовый URL картинок.                                        |
| `VITE_TMDB_LANGUAGE`      | нет         | Язык ответов TMDB. По умолчанию `ru-RU`.                            |
| `VITE_ASSISTANT_ENDPOINT` | нет         | URL собственного бэкенда SYO для AI. По умолчанию `/api/assistant`. |

Ключа AI-провайдера в этой таблице нет и быть не может: всё с префиксом `VITE_`
попадает в публичный бандл. Ключ живёт только на сервере — см.
[`server/README.md`](server/README.md).

\* Нужен либо `VITE_TMDB_API_KEY`, либо `VITE_TMDB_ACCESS_TOKEN`. Без них
приложение запускается и показывает локальный кэш, а в Movie Picker выводит
понятное сообщение о ненастроенном токене — оно не ломает экран.

### Где взять TMDB-токен

1. Регистрация на https://www.themoviedb.org/.
2. Settings → API → Create → Developer.
3. Скопировать **API Key (v3 auth)** в `VITE_TMDB_API_KEY`.

`.env` в `.gitignore`. Ключ TMDB попадает в клиентский бандл — это допустимо
только для публичного read-only ключа. Приватные ключи так публиковать нельзя.

---

## Публикация на GitHub Pages

```bash
npm run deploy:pages
```

Собирает бандл с базовым путём `/syo/` (без sourcemap) в `docs/`, кладёт рядом
`404.html` — копию `index.html`, чтобы прямые ссылки вида `/syo/film/693134`
не отдавали 404, — и `.nojekyll`. Папка `docs/` коммитится: в настройках
репозитория Pages → _Deploy from a branch_ → `main` / `/docs`.

Итоговый адрес: `https://<аккаунт>.github.io/syo/`.

Другой подпуть — `VITE_BASE=/другой/ npm run build`. Маршруты сами
подстраиваются под базу (`src/app/routes.ts` читает `import.meta.env.BASE_URL`),
поэтому ссылки не выходят за пределы приложения.

## Проверка в Telegram

1. Опубликовать сборку по HTTPS (см. выше; Telegram открывает только HTTPS).
2. В @BotFather: `/mybots` → бот → _Bot Settings_ → _Menu Button_ → указать URL.
3. Открыть бота в Telegram и нажать кнопку меню.

Локальная проверка на телефоне без деплоя: `npm run dev` слушает `0.0.0.0`, но
Telegram требует HTTPS — понадобится туннель (`cloudflared tunnel --url http://localhost:5173`
или аналог) и указание HTTPS-URL туннеля в BotFather.

### Fallback в обычном браузере

Приложение полностью работает без Telegram: `TelegramController` определяет
отсутствие WebApp SDK и переключает safe-area на `env(safe-area-inset-*)`,
высоту — на `100dvh`, режим chrome — на «свой back», haptics — в no-op.
Роутинг настоящий, ссылки вида `/film/693134` открываются напрямую.

---

## Скрипты

| Команда                    | Что делает                                         |
| -------------------------- | -------------------------------------------------- |
| `npm run dev`              | Дев-сервер Vite.                                   |
| `npm run build`            | Проверка типов + продакшн-сборка в `dist/`.        |
| `npm run preview`          | Локальный просмотр собранного бандла.              |
| `npm run lint`             | ESLint, включая архитектурные запреты (см. ниже).  |
| `npm run format`           | Prettier.                                          |
| `npm test`                 | Vitest: unit + интеграционные (jsdom).             |
| `npm run test:watch`       | Vitest в watch-режиме.                             |
| `npm run test:e2e`         | Playwright E2E (сам собирает и поднимает preview). |
| `npm run test:e2e:install` | Скачать Chromium для Playwright (один раз).        |

---

## Архитектура

```
src/
  app/            — оболочка: навигация, Telegram, тема, performance, сеть
  pages/          — экраны: feed, movie-picker, film, diary, profile
  entities/       — модели и репозитории: film, feed, watchlist
  shared/         — api/tmdb, ui, motion, haptics, images, storage, utils, types
  styles/         — reset, global, typography
```

### Слои и правила

**UI** отображает состояние и отправляет семантические действия. Он не знает
Telegram API, не знает IndexedDB, не строит URL для TMDB. Эти запреты не просто
договорённость — они проверяются ESLint-правилами для `src/pages/**` и
`src/shared/ui/**`: прямой `fetch`, `localStorage`, `window.Telegram`,
`querySelector` там дают ошибку линта.

**Repositories** (`entities/*/*.repository.ts`) объединяют локальный кэш и сеть,
нормализуют данные и возвращают модели приложения.

**Controllers** (`app/*`):

- `TelegramController` — единственный владелец Telegram WebApp API: `ready()`,
  `expand()`, версия и платформа, viewport, safe-area, fullscreen, BackButton,
  системные цвета, haptics. Публикует нормализованное состояние и CSS-переменные.
- `NavigationController` — стек маршрутов, history браузера, видимость
  Telegram BackButton, одноразовый запрос fullscreen, семантические haptics.
- `PerformanceController` — измеряет реальный ритм кадров и понижает
  «тир» декораций (parallax → blur → ambient), не трогая данные.
- `ConnectivityController` — online/offline и разбор очереди отложенных записей.
  Постоянной плашки «Offline» нет: индикатор (`shared/ui/SyncIndicator`)
  появляется только когда синхронизация идёт дольше 2,5 с или осталась реальная
  ошибка. Сам по себе офлайн ничего не показывает — приложение работает из
  IndexedDB.

**Stores** (Zustand): активный root-таб, стек маршрутов, состояние Telegram,
тема, watchlist, снекбар, performance-тир, сеть.

**Remote data** — TanStack Query. **Persistent data** — IndexedDB через Dexie
(`films`, `feed`, `presentations`, `watchlist`, `preferences`, `syncQueue`).

**Внешние данные** проходят через Zod — и сетевые ответы TMDB, и то, что читается
из IndexedDB: испорченная запись кэша не роняет экран, а просто считается
отсутствующей.

### Telegram-first: viewport и safe-area

Жёсткого верхнего отступа в 64 px нет нигде. `TelegramController` пишет в
`<html>`:

```
--tg-viewport-height   --safe-top/right/bottom/left
--content-safe-*       --tg-controls-keepout-right   --keyboard-height
```

`--content-safe-*` — **абсолютные** значения: `safeAreaInset + contentSafeAreaInset`.
Компонент применяет только одну переменную и не может учесть inset дважды.
Вне Telegram эти же переменные получают `env(safe-area-inset-*)`, поэтому
логика вёрстки не ветвится.

`--tg-controls-keepout-right` отодвигает правые элементы от кластера
close/menu, когда Telegram не зарезервировал под него вертикальную полосу.

### Два режима chrome

| Режим                           | Наша кнопка «назад» | Telegram BackButton |
| ------------------------------- | ------------------- | ------------------- |
| A. Fullscreen / обычный браузер | видима              | скрыт               |
| B. Telegram chrome              | не рендерится       | видим               |

Правило живёт в одном месте — компоненте `shared/ui/BackControl` плюс
`TelegramController.setBackButtonVisible()`. Двух кнопок «назад» одновременно
быть не может по построению.

### Film Presentation Preflight

Preflight запускается **до push маршрута**, а не после монтирования страницы.
Координатор — `pages/film/filmOpening.ts`:

```
тап по карточке
  ├─ startFilmOpening(summary)   // detailed → logo → decode → контраст → freeze
  └─ navigation.openFilm(...)    // push маршрута, не дожидаясь preflight
```

Push не ждёт preflight намеренно: блокировать тап на бюджет означает добавить
input lag каждому холодному открытию ради нескольких кадров пустого заголовка на
тёплом. Данные не ждут анимацию, анимация не ждёт данные. Preflight успевает
отработать во время перехода страницы.

`FilmPage` забирает **тот же** promise через `takeFilmOpening(filmId)` — один
preflight на открытие, композиция hero не пересчитывается на лету. Прямая ссылка
и перезагрузка страницы координатора не имеют, поэтому там preflight запускает
сама страница — та же функция.

`pages/film/film.presentation.ts` до отрисовки hero:
получает detailed-данные → выбирает logo-кандидата (`film.logo.ts`) → грузит и
декодирует его → проверяет контраст через canvas (`shared/images/logoClassifier.ts`)
→ выбирает режим logo/text → готовит preview постера и бэкдропа → возвращает
**замороженный** объект.

Пока решение не принято, заголовок не рисуется вовсе (в DOM остаётся только
доступный `<h1>` для скринридера). Поэтому текст не мелькает перед логотипом.
Если логотип не успел в бюджет — hero фиксируется на тексте до следующего
открытия; решение кэшируется в IndexedDB.

Бюджет — **1400 мс**. Логотип весит около 8 КБ в размере w300, но лежит за
полным круговым обходом до CDN: замерено 380–480 мс до `image.tmdb.org` на
быстром десктопном канале и в 2–3 раза больше на мобильном, где рукопожатие
холодное. Прежние 550 мс проходили на десктопе и не проходили на телефоне —
именно поэтому там названия регулярно оказывались текстом. Загрузка при
таймауте не отменяется, поэтому она всё равно догревает HTTP-кэш. Плюс
`preconnect` к `image.tmdb.org` и `api.themoviedb.org` в `index.html` убирает
DNS и TLS с критического пути первой картинки.

Тона логотипа: `light` — как есть; `dark-monochrome` — инвертируется;
`dark-colored` — обесцвечивается и осветляется; `unsafe` (в том числе
tainted canvas из-за CORS) — текстовый заголовок. Текстовый заголовок всегда
тёпло-белый и никогда не наследует цвет изображения.

### Параллакс

Считается в `requestAnimationFrame`, пишется прямо в `style.transform`.
React-состояние на кадр не обновляется.

- Лента: смещение считается от положения карточки во вьюпорте, а не от
  `scrollTop`; двигается только слой изображения, текст стоит; у изображения
  есть overscan, поэтому край не обнажается.
- Film Page: бэкдроп ~0.42 от обычной скорости, постер ~0.68 (быстрее бэкдропа),
  текст — 1.0, ambient следует за бэкдропом.
- Reduce Motion не выключает глубину полностью, а оставляет минимальную.

### Оттягивание страницы фильма

Изображение растёт, пока тянут, и **потолка нет** — ни настоящего, ни
заметного. Линейная шкала с обрезкой давала стену: после условных 180 px
картинка замирала и жест становился мёртвым. Вместо неё логарифм
(`pullGrowth` в `shared/utils/parallax.ts`): рост не прекращается никогда, но
каждый следующий миллиметр стоит непропорционально большей оттяжки — то есть
дальше тянуть просто тяжелее.

Кривая нормирована так, что привычная первая часть жеста не изменилась:
180 px по-прежнему дают ровно прежнюю величину. Замерено на живой странице:

| Оттяжка | Масштаб | Прибавка                   |
| ------- | ------- | -------------------------- |
| 0       | 1,180   | —                          |
| 90 px   | 1,274   | +0,094                     |
| 180 px  | 1,340   | +0,066                     |
| 360 px  | 1,434   | +0,094 за следующие 180 px |
| 720 px  | 1,552   | +0,118 за следующие 360 px |
| 1400 px | 1,681   | +0,129 за следующие 680 px |

Оттяжка читается из двух источников, потому что они не сосуществуют: iOS
отдаёт отрицательный `scrollTop`, Android обрезает его на нуле и говорит
только палец.

### Бэкдропы и кадрирование

Вертикальный постер **никогда** не растягивается на задний план — ни в Ленте, ни
на Film Page, ни в presentation cache. Если `backdropPath` отсутствует,
`ImageStage` остаётся в атмосферном состоянии: dominant color, мягкий градиент и
два ambient-пятна. Иконки ошибки нет.

Известный техдолг: крупная карточка Ленты — вертикальный контейнер 4:5, а внутрь
кладётся горизонтальный backdrop с `object-fit: cover`, поэтому у части фильмов
смысловой центр уезжает за кадр. TMDB не отдаёт focal point, safe-crop area или
saliency-данные — `/movie/{id}/images` возвращает только пути, размеры и голоса,
— поэтому центрировать не на что без выдуманной эвристики. Оставлено
центрированным до отдельного этапа image art-direction: выбор среди всех
backdrops, per-film `object-position` или saliency-детекция. Подмена постером
решением не является и запрещена.

### Ошибки

Ошибка блока не заменяет экран: нет cast — блок исчезает; нет бэкдропа —
атмосферный фон из dominant color; нет логотипа — текст; нет постера — типографский fallback с
названием и годом (без иконки ошибки и без слова SYO); нет деталей — остаётся
кэш. Полноэкранная ошибка появляется только если нет ни локальных, ни сетевых
данных: «Не получилось загрузить информацию о фильме.» и кнопка «Повторить».

---

## Ограничения

- **Swipe-back намеренно отсутствует.** Нет edge-zone, pointer-хендлеров назад,
  GestureCoordinator, interactive progress, drag-transform, commit/cancel,
  velocity, клонов постера и page-to-poster morph при закрытии. Выход со
  страницы фильма — только кнопкой: своей в fullscreen, Telegram BackButton вне
  fullscreen. Временный переход: 190–220 мс, сдвиг вправо на 8 px, opacity → 0,
  `scale` всегда 1, нижний экран не масштабируется, blur не меняется, haptic нет.
  Этот переход нельзя называть Apple TV-like.
- Текст записи, AI, «Почерк», анализ вкуса, повторные просмотры, share и social
  не реализованы: на этом этапе единственный законченный вид записи — оценка без
  текста. Полный Профиль тоже впереди.
- Detailed → quick не поддерживается намеренно: действия, которое стирает разбор
  по пяти аспектам, в интерфейсе нет.
- **Fullscreen не подтверждён на реальном устройстве.** Код запрашивает его один
  раз после первого осознанного действия и покрыт e2e с фейковым Telegram, но
  это доказывает поведение кода, а не поведение клиента. Чтобы проверить:
  открыть мини-апп в Telegram → вкладка «Профиль» → блок «Журнал Telegram». Там
  видно `ready+expand · <платформа> v<версия>`, `requestFullscreen() called` и
  пришёл ли в ответ `fullscreenChanged → on`. Если версия клиента ниже 8.0,
  строка будет `fullscreen unsupported` — это штатный fallback, а не ошибка.
- Профиль — качественный placeholder: выбор темы и честный статус среды.
- Серверной синхронизации нет: очередь `syncQueue` копится локально и
  разбирается no-op транспортом. Точка подключения бэкенда — `SyncTransport`
  в `app/connectivity/ConnectivityController.ts`. Записи Дневника ставятся в эту
  же очередь (`journalUpsert` / `journalDelete`) и хранятся со статусом `local`.
- Виртуализации списка Дневника нет: при десятке записей она не нужна, а
  архитектура (селекторы, стабильные размеры постеров) её не блокирует.
- Из семантических haptic-событий в v1 реально срабатывают `tabSelection`,
  `movieOpen`, `bookmarkAdd`, `bookmarkRemove`, `refreshNewContent` и
  `criticalError`. `pullThreshold` объявлен и покрыт тестом, но не вызывается:
  pull-to-refresh как жест в этой версии не реализуется.
- Аватар в шапке ленты — заглушка, профиль Telegram не читается.
- Тема System маппится на Cinema (тёмная) / Graphite (светлее); отдельной
  светлой темы нет — иерархия важнее следования системе буквально.

---

## Оценка и Дневник (P0.2)

Полная цепочка: Лента/Поиск → страница фильма → «Начать оценку» → выбор режима →
оценка → итог → сохранение → карточка в Дневнике → просмотр, изменение, удаление
с Undo. Всё работает офлайн и переживает перезапуск.

### Два режима

**Быстрая оценка** — одно значение 0–5. **Разобрать впечатление** — пять
аспектов в фиксированном порядке: Сюжет, Герои и актёрская игра, Режиссура и
визуал, Звук и музыка, Что осталось. Итог считается приложением и вручную не
правится:

```
rawAverage   = (сумма пяти аспектов) / 5
displayScore = round(rawAverage * 2) / 2      // шаг в половину звезды
```

Хранится и исходное значение каждого аспекта, и `rawAverage`, и `displayScore`,
и версия формулы — не только отрисованное число.

`null` («ещё не оценивал») и `0` («осознанно поставил ноль») нигде не смешиваются:
в коде нет ни одной проверки оценки на falsy. До первого действия звёзды пустые,
подпись «Проведи по звёздам», сохранение недоступно.

### Маршруты и состояние

```
/rate/:filmId/mode              выбор режима
/rate/:filmId/quick             быстрая оценка
/rate/:filmId/aspects/:aspectId один из пяти аспектов
/rate/:filmId/result            итог
/journal/:entryId               сохранённая запись
```

Аспекты меняются через `replaceTop`, поэтому весь flow занимает одну запись
истории: «назад» из него возвращает на страницу фильма, а не отматывает аспекты
по одному. Шаг назад внутри flow делает перехватчик
(`NavigationController.setBackInterceptor`) — через него проходят и своя кнопка,
и Telegram BackButton, так что реализация ровно одна.

Машина состояний — чистые функции в `domain/rating/rating.machine.ts`. Текущий
шаг живёт в домене и в хранилище, из DOM он не выводится. Прямая ссылка на
`/result` без данных не рисует сломанный экран: guard уводит на первый
незаполненный аспект.

### Черновик

Активный черновик ровно один на всё приложение. Он создаётся **после** выбора
режима — открыть селектор и уйти не оставляет пустой черновик. Хранится в двух
слоях (`features/drafts/activeDraft.repository.ts`):

- **IndexedDB** — основной;
- **localStorage** — синхронный снимок после каждого коммита, без картинок и
  TMDB-данных.

При старте читаются оба и побеждает больший `revision`: если WebView убили между
синхронной записью и асинхронной, последний шаг всё равно на месте. Попытка
начать другой фильм открывает лист выбора, а не подменяет черновик молча.

### Дневник

Grid и list с сохраняемым выбором, группировка по месяцу `createdAt` (это дата
добавления записи, а не просмотра — редактирование не переносит карточку в другой
месяц). Один фильм — одна активная запись; редактирование обновляет её и не
создаёт дубль. Удаление мягкое: строка остаётся, поэтому Undo — это
восстановление на прежнее место, а не повторная вставка.

### Миграция данных

`shared/storage/db.ts` версия 2 добавляет два хранилища:

```
ratingDrafts: 'id, updatedAt'
journal:      'id, filmId, createdAt, deletedAt, syncStatus'
```

Dexie применяет только недостающие версии, а хранилища, не упомянутые в
`version(2)`, не трогаются — кэш фильмов и «Посмотреть позже» переживают
обновление (покрыто тестом). `filmId` индексируется, но **не** уникален:
у удалённой записи остаётся строка-надгробие, и уникальный индекс ломался бы при
повторной оценке. Единственность активной записи обеспечивает репозиторий.

## Тесты

```bash
npm test          # 429 unit + integration, 37 файлов (включая бэкенд)
npm run test:e2e  # Playwright, 339 сценариев: iPhone, Android, Reduce Motion
```

Unit покрывают: маппер TMDB, выбор логотипа, классификатор контраста,
репозиторий watchlist, нормализацию Telegram-инсетов, разрешение темы,
HapticManager (cooldown/дедупликация/отключение), ImagePipeline, параметры
временного back-перехода, разбор очереди синхронизации и правила показа
индикатора.

Integration (jsdom, поверх реального `<App/>`): старт с кэшем, открытие
Movie Picker, поиск, открытие фильма, стабильность logo/text, закладка,
back (своей кнопкой и Telegram BackButton), офлайн-фильм, блочная ошибка.

E2E (Playwright, с моками TMDB и Telegram WebApp): fullscreen и non-fullscreen,
незакрытые controls, длинное название, отсутствующий постер, чёрный логотип,
медленный логотип, отсутствие логотипа, клавиатура, viewport Android и iPhone,
Reduce Motion.

P0.2 добавляет к этому: формулу и округление до половины звезды, различение
`null`/`0`, машину состояний черновика (нельзя пропустить аспект, нельзя открыть
итог раньше времени, один активный черновик, конфликт при другом фильме),
миграцию v2 и сохранность watchlist, идемпотентность сохранения, единственность
записи на фильм, сохранение `createdAt` при редактировании, мягкое удаление и
Undo, восстановление черновика из более свежего слоя, `StarRatingControl`
(tap, drag в обе стороны, ноль, отсутствие значения по умолчанию, клавиатура,
ARIA, отсутствие записи в черновик на каждый пиксель, один эффект максимума), а в
E2E — прохождение flow только с клавиатуры, drag по звёздам, переживание
перезагрузки, посадку в Дневник, удаление с Undo, скрытый нижний бар и CTA выше
системного инсета.

## Текст, разговор и AI (P0.3)

### Один черновик — двух видов

Оценка и текст делят один слот хранилища, поэтому `ActiveDraft = RatingDraft |
WritingDraft` (`domain/writing/writing.types.ts`). Черновик текста узнаётся по
полю `kind: 'writing'`; у черновика оценки его нет — так старые записи читаются
без миграции.

Любой вход — начать оценку, отредактировать запись, начать или продолжить
текст — проходит через `features/drafts/draftCoordinator.ts`. Он отвечает на
один вопрос: есть ли уже черновик и чей он. Ответы: `started`, `resumed`,
`conflict`. Незаконченный текст — такая же работа, как незаконченная оценка:
начать оценку поверх него нельзя без явного подтверждения, и наоборот.

Удаление черновика любого вида делает `discardActive()`, возврат — `restoreDraft()`
со снимком: возвращается ровно то, что было, а не пересозданное подобие.

### Редактор

Текст виден сразу и пишется с задержкой в 800 мс: запись на каждое нажатие
клавиши била бы по IndexedDB, а запись раз в минуту теряла бы абзац. Любой
выход — смена экрана, blur, `pagehide`, `visibilitychange`, уход из потока —
сбрасывает отложенную запись немедленно (`writing.store.ts`, `flush()`).

Счётчик символов молчит до 27 000 из 30 000: раньше он был бы шумом. Позиция
курсора и прокрутка запоминаются, но сами по себе записи не вызывают.

### Ревизии

Ревизия появляется, только когда текст действительно изменился, а не на каждое
открытие редактора. Первый текст человека сохраняется как ревизия в момент
принятия предложения SYO — иначе именно это и было бы моментом, когда оригинал
исчезает. Все версии доступны в листе «Версии текста», включая самую первую.

### Разговор

Вопросы приходят по одному. Ответ сохраняется **до** любого запроса — своими
словами, дословно. Пропуск — не пустое мнение: `answerText: null`, и в запрос
такой ход не попадает. Собрать текст можно только при хотя бы одном ответе:
собирать не из чего — значит выдумывать мнение.

Первый вопрос запрашивается автоматически ровно один раз; следующие — только
явным действием (ответить, пропустить, «другой вопрос»). Неудача переводит
разговор в статус `error` и не повторяется сама: одна мёртвая сеть должна стоить
один запрос, а не бесконечный поток.

### Кандидат, а не замена

Любая операция SYO (`collect`, `correct`, `shorten`, `connect`) даёт
`AssistantCandidate`. Рабочий текст не меняется, пока человек не нажал
«Принять». Есть «Оставить свой вариант» и «Редактировать вручную», и оба
варианта можно прочитать рядом.

Ответ принимается, только если совпали `requestId` **и** ревизия, от которой
запрос строился (`isCandidateFresh`). Опоздавший ответ на вопрос, который
человек уже прошёл, молча отбрасывается. Ошибка запроса ничего не меняет в
черновике и снимает только собственную отметку `pending` — иначе брошенный
запрос, завершившись позже, стирал бы отметку того, которого ждут сейчас, и
свежий ответ пропадал бы как устаревший.

### Безопасность AI

Браузер обращается только к собственному бэкенду SYO (`features/writing/gateway/`).
В бандле нет ни ключа, ни имени провайдера, ни промптов. `initData` уходит одним
заголовком и проверяется на сервере (HMAC, `auth_date`, сравнение за постоянное
время). Полный текст рецензии, ответы и содержимое ответа модели не попадают ни
в один лог. Мат и резкость сохраняются: это стиль, а не ошибка.

Развёртывание бэкенда, переменные окружения и чек-лист — в
[`server/README.md`](server/README.md). Таблица приёмки этапа —
[`P0_3_ACCEPTANCE.md`](P0_3_ACCEPTANCE.md).

### Текст в Дневнике

`DiaryEntry` получил `hasText` и `text: DiaryText | null`. Миграция v4 добивает
существующие записи (`text = null`, `hasText = false`) — оценки не трогает.
`DiaryRepository.saveText()` пишет текст в той же транзакции, в которой читает
запись, поэтому оценка, сохранённая параллельно, не откатывается. Удаление
текста оставляет оценку и предлагает Undo.

На карточке — настоящая выдержка из написанного, никогда не пересказ; текст со
спойлером показывает только, что он есть. Пользовательский текст всегда
рендерится как обычный текст: ни `dangerouslySetInnerHTML`, ни markdown.

## Адаптивность и постеры (P0.3.1)

### Кто владеет размером

`Poster` отвечает за картинку: загрузку, preview, fallback, пропорцию,
`object-fit`. Ширину задаёт **рамка родителя** — элемент с
`data-poster-frame`, у которого есть своя ширина и `aspect-ratio: 2/3`.

`requestWidth` — это подсказка о пикселях: она уходит в `ImagePipeline` и в
атрибуты `width`/`height` у `<img>` (чтобы браузер знал пропорцию до загрузки),
и **никогда** не превращается в CSS-ширину. До P0.3.1 одно число делало обе
работы: постер в 160px стоял в треке шириной 112px, наезжал на соседа и уходил
за экран.

### Сетка Дневника

Колонки считаются от доступной ширины контента, а не от ширины окна:

```css
grid-template-columns: repeat(auto-fit, minmax(min(100%, 100px), 1fr));
```

Две колонки на 280–320px, три на 360–430px, больше — на широком окне. Вся
цепочка `ul → li → card → frame` умеет сжиматься (`min-width: 0`), поэтому один
жёсткий ребёнок больше не выталкивает соседей.

### Плотность карточки

В сетке — постер, название, оценка и маленький маркер «есть текст». Выдержка
живёт в списке: под постером шириной 110px две строки прозы превращают сетку в
стену рваного текста.

### Ширина на десктопе

Токены `--content-max-reading: 640px`, `--content-max-feed: 720px`,
`--content-max-diary: 760px`. Mini App не превращается в узкую колонку на
большом экране, но и одна карточка не растягивается на 1200px.

### Проверка

`e2e/responsive-layout.spec.ts` меряет настоящие прямоугольники: пересечение
соседей, выход за viewport, совпадение границ постера и рамки (±0.5px),
пропорцию 2/3, ярлыки внутри кнопок при 150% и 200% текста. Старая проверка
`document.scrollWidth` этого не ловила: body фиксирован, root обрезает, и
глобальная ширина остаётся равной viewport, пока карточки лежат друг на друге.

Скриншоты и видео ресайза: `docs/screenshots/`, `docs/video/`. Таблица приёмки
этапа — [`P0_3_1_ACCEPTANCE.md`](P0_3_1_ACCEPTANCE.md).

## Лента (P0.4)

### Что это отвечает

«Что нового и лично значимого появилось вокруг моего киноархива?» — а не «вот
популярное». Один поток: рекомендации, наблюдения, вехи, возвраты из
«Посмотреть позже» и нейтральная discovery, когда сказать пока нечего.

### Ничего не утверждается без доказательства

Наблюдения считаются локально и детерминированно (`domain/feed/insight.engine.ts`),
у каждого есть порог выборки и список фильмов-доказательств. Сырой текст
рецензий не анализируется — движку доступны только факт наличия текста и его
длина. Не хватило данных — наблюдения не будет: пустая лента честнее
выдуманного вывода.

Рекомендация всегда называет источник: «Похож на "Дюну", который ты оценил
высоко». Формулировка про режиссёра появится только тогда, когда его фильмов в
Дневнике действительно несколько. «Почему это здесь?» показывает ту же причину
и даёт «Неинтересно» с Undo.

### Шесть композиций, а не одна карточка с меняющимся текстом

Кинематографичная рекомендация, коллекция-рельс, наблюдение с раскрытием
доказательства, веха, возврат из списка и discovery — отдельные компоненты с
общими примитивами. Правила чередования (`enforceSequence`) применяются ко
всему видимому списку: обновление больше не кладёт пачку рекомендаций поверх
истории.

### Порядок и позиция

Идентификаторы элементов детерминированы, поэтому обновление не пересоздаёт
то, что не изменилось. Позиция запоминается якорной карточкой и её смещением;
новые элементы сверху компенсируются по высоте, и то, что человек читает,
остаётся на месте.

### Жесты

До 10px направление не решено — лента скроллится. Горизонталь забирается
только при явном перевесе, и только тогда ставится pointer capture. Свайп
вправо — в «Посмотреть позже», влево — «Неинтересно»; оба действия есть
кнопкой и в меню, жест никогда не единственный доступ.

Подробности, пороги наблюдений, объяснение ранжирования и таблица приёмки —
в [`P0_4_ACCEPTANCE.md`](P0_4_ACCEPTANCE.md).

## Профиль и «Почерк» (P0.5)

Профиль отвечает на два вопроса: что я сохранил о себе и какой киноязык
складывается из моих оценок. Всё считается на устройстве.

`TasteProfileEngine` — чистая функция от Дневника и кэша фильмов. У каждого
вывода есть порог выборки и список фильмов-доказательств; ниже трёх оценок не
утверждается ничего, ниже пятнадцати всё сказано через «пока». Текст записей не
читается — движку доступны только факт наличия и длина.

Настройки меняют подачу, а не данные: тема, движение (системный Reduce Motion
всегда сильнее), тактильный отклик, режим помощи SYO. Экспорт складывает архив
в файл на устройстве и никуда его не отправляет; удаление требует двух явных
шагов и предлагает сначала экспортировать.

Пороги, шаблоны и таблица приёмки — [`P0_5_ACCEPTANCE.md`](P0_5_ACCEPTANCE.md).

## Полировка: движение, тактильность, единый язык интерфейса

Длительности живут в одном месте — `src/app/theme/tokens.css`. Ни один модуль
стилей не содержит миллисекунд: он выбирает ступень (`--duration-fast`,
`--duration-slow`, …), а каждая ступень умножается на `--motion-scale`. Шкалу
выбирает один атрибут на `<html>`: `full` — 1, `calm` — 0.6, `reduced` — 0.01.
Системный Reduce Motion всегда сильнее настройки в приложении, и та же доля
уходит в JS-переходы страниц, так что настройка не может примениться к половине
экранов.

Тактильность — семантическая: у события одно значение, один паттерн и порог в
120 мс. «Деликатный» режим оставляет только то, что подтверждает действие
пользователя. Жесты договариваются по оси, а не по порядку слоёв: горизонталь
забирает указатель только после 10 px и превышения вертикали в 1.6 раза.

Постоянный аудит §53 — [`e2e/master-polish.spec.ts`](e2e/master-polish.spec.ts):
шесть экранов на пяти ширинах, 44 px, 200 % текста, Reduce Motion. Инвентарь
компонентов, таблицы токенов, тактильности и жестов, а также честный список
отложенного — [`MASTER_POLISH_ACCEPTANCE.md`](MASTER_POLISH_ACCEPTANCE.md).

---

## Следующий этап

Синхронизация: очередь задач уже пишется в одной транзакции с данными,
`revision` и `clientMutationId` готовы к разрешению конфликтов, транспорт пока
не подключён. Отдельным этапом — интерактивный Apple TV-like swipe-back, для
которого специально оставлены: единая точка выхода (`NavigationController.goBack`
плюс перехватчик), стек маршрутов, отдельный слой страницы в `AppShell` и
параметризованные переходы в `shared/motion/transitions.ts`.
